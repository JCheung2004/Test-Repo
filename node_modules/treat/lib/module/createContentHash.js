import crypto from 'crypto';
export var createContentHash = function (content) {
    return crypto
        .createHash('sha1')
        .update(JSON.stringify(content))
        .digest('base64')
        .slice(0, 5)
        .replace(/^((-?[0-9])|--)/, '_$1');
};
